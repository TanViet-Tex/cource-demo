export const statusCatalogQueryApi = {
  getAllStatusCatalog: async () => {
    // minimal stub returning empty array to satisfy imports during iteration
    return [] as Array<{ id: number; statusName: string }>
  },
}
